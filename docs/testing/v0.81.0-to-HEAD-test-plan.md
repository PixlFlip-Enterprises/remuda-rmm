# Release Test Plan — unreleased since v0.81.0

- **Baseline release:** `v0.81.0` (published GitHub release, 2026-06-16, tag commit `9b23bc66`)
- **Scope:** 59 commits on `main` not reachable from `v0.81.0`
- **Generated:** 2026-06-19
- **Creds (local):** `admin@breeze.local / BreezeAdmin123!`, web on `:4321`, `PUBLIC_API_URL=http://localhost`

Pure docs / CI / dep-bump / test-only commits are listed at the bottom under "Excluded — no manual test needed."

---

## ✅ Session results — 2026-06-19 (pass 1)

Evidence log: `docs/testing/FEATURE_TEST_LOG.md` (UI QA Sweep + Patching Area Sweep + Windows agent sections).

**UI sweep — verified PASS:** Quotes P1 editor/PDF (#1455), Quotes P2 public view (#1468), billing-catalog UI (#1467), invoice in-flight state (#1460), Stripe API-key entry (#1610), device software search (#1461), Monaco VT theme (#1593) / Script-AI insertion (#1453) / library tools (#1457), Update Ring matrix editor (#1456), partner-wide alert templates + picker gating (#1466/#1469), collapsed panels inert (#1463), zod v4 sweep (#1451/52), SDK 0.3 tools (#1484).

**Bugs found → fixed → MERGED:**
- #1628 — quote "Due on acceptance" total (was misleading $1,950 vs $500 invoiced)
- #1629 — billing RBAC sidebar gating + access-denied state (#1454 follow-up)
- #1630 — public quote-accept page hydration (portal CSP) + malformed acceptUrl (#1468)
- #1632 — calm "not enabled" state for disabled identity integrations
- #1635 — **feat:** Pax8 UI + Distributors tab; TD SYNNEX moved onto the Integrations hub
- #1636 — Patching: respect-the-org-switcher scoping + silent-failure/feedback/confirm fixes

**Windows agent (built current-main, installed on the `.55` Tailscale box, enrolled to local stack):**
- #1524 BIOS/GPU/motherboard reporting — **PASS** (agent→API→DB→device-detail API)
- #1478 pending_reboot clears after reboot — **PASS**
- #1390 watchdog version in UI — **PARTIAL** (manual install ordering; needs MSI-style watchdog-supervises-agent handshake)

**Still BLOCKED (need a Windows MSI build host + interactive UAC console):** #1480 (UAC double-click), #1477 (`/api` pre-flight), #1613 (ProgramData ACL self-heal).

**Pending live-browser confirmation (best against freshly-rebuilt merged `main`):** #1629 per-role sidebar, #1630 real-token accept click-through, #1636 patches org-switcher behavior + un-dimmed switcher, #1635 Pax8/Distributors tab render, #1632 not-enabled state render. The pass-1 stack served pre-fix `main`, so these were verified via unit tests + data/JWT checks, not pixels.

**Seeded local test data (left in DB):** 3 orgs under Default Partner (Default Organization, Northwind IT, Acme Managed Services); users `billing@breeze.local` (Partner Billing) + `tech@breeze.local` (Partner Technician), both `BreezeAdmin123!`; one live Windows device `WIN-DHQNR1F8LO2`.

---

## ✅ Session results — 2026-06-19 (pass 2)

Ran against a freshly-rebuilt merged `main` (all 6 pass-1 PRs present), so the pass-1 "pending live-browser confirmation" items were verified against the *real running fixes* this time. Full evidence: `docs/testing/FEATURE_TEST_LOG.md` (Pass 2 sections).

**GOAL A — live-browser confirmation of the 6 merged pass-1 fixes:**
- **#1636** patches org switcher — **PASS** (un-dimmed; All-orgs disables Export/New Ring with "Select an organization…" hints, no 400s; Current re-enables).
- **#1635** Distributors tab — **PASS** (Pax8 + TD SYNNEX panels render; TD SYNNEX removed from Settings→Catalog).
- **#1632 / #1628** — **PASS** (Google Workspace + M365 calm "not enabled" states; quote "Due on acceptance" = one-time total, verified $500 quote → portal → invoice INV-2026-0003).
- **#1629** billing RBAC — **PARTIAL → fixed.** Per-role sidebar item-gating correct, BUT 403s rendered as "Session expired"/"Failed to fetch" instead of access-denied (AccessDenied only wired into Roles+Invoices) + empty nav group headers for billing. **Fixed + MERGED in #1640.**
- **#1630** public quote accept — **MIXED → fixed.** Accept page SSR-renders with no CSP violations; accept→auto-issue→convert verified via API (INV-2026-0003). BUT acceptUrl emitted as `https:///portal/quote/<token>` (dead host) — `portalBase()` missed the `https:///portal` empty-authority form. **Fixed + MERGED in #1639.** (Local dev island-hydration 404 is an Astro base-path dev artifact, likely prod-safe — worth a prod-build confirmation.)

**GOAL B — broad sweep (26 nav areas pass 1 didn't cover): 25 PASS · 1 PARTIAL · 0 FAIL.** Confirmed no-regression on AI Assistant streaming+tools (#1591/#1484), partner-wide Custom Fields write (RLS #1611), Reports/Analytics charts. Bugs:
- Partner-settings flash-of-access-denied [MED] — **fixed PR #1642.**
- `GET /alerts/routing-rules` 400 on the All-Orgs landing [#1636 orgId class] — **fixed PR #1643.**
- Notification-channel "Test" silent mutation [MED] — **FALSE POSITIVE** (re-verified in-browser: success toast + "Last test" timestamp DO appear; sweep missed the short-lived toast).

**Pass-2 PRs:** #1639, #1640 MERGED; #1642, #1643 opened, merging on green required-checks.

**Left for triage (not fixed):** org-create slug not auto-derived [LOW]; `?tab=` query-param vs `#hash` drift on Monitoring/Discovery (CLAUDE.md hash-only); stale "API 0.63.5" sidebar footer version string.

---

## 🔴 Cross-cutting regression risk (test broadly, not just the feature)

| PR | Change | Why it's high-risk |
|---|---|---|
| #1451 / #1452 | **Zod 3 → 4 upgrade (1/3, 2/3)** | Most v4 breaks are *silent* (no type error): `.uuid()`→`.guid()`, `.int()` 2⁵³ cap on uint64 byte counters, `ZodError.issues` non-enumerable breaking 400 messages, `.partial()` injecting defaults. Touches every validator. Exercise a wide spread of create/edit forms and confirm 400 error bodies still render field messages. |
| #1484 | **claude-agent-sdk bump to 0.3 + tool handler type port** | AI agent tool-call path. Smoke every AI tool category (device, network, scripts). |
| #1610 | **Per-partner Stripe API-key model — replaces Connect** | Replaces the entire payments substrate. Every billing pay flow (quotes, invoices, checkout) depends on it. |

## 🟢 Backend / API / integration (covered by API + integration suites; spot-check)

| PR | Change | Verify |
|---|---|---|
| #1616 (#1448) | Stripe Checkout moved **outside** request DB transaction | No pool poisoning; checkout session still creates |
| #1609 (#1608) | Analytics query failures **surface instead of fake-empty 200** | Force a failing analytics query → user sees an error, not empty chart |
| #1591 | AI approval poll read 0 rows under RLS (always "rejected/timed out") | Approve an AI tool → it actually proceeds (was the US 6/18 bug) |
| #1594 | **Pax8 license sync** | Partner-axis RLS; sync runs without 42501 |
| #1596 | **TD SYNNEX catalog import** | Import path + catalog rows land |
| #1614 (#1612) | RLS coverage: exempt Pax8 partner-axis tables | Contract/integration test only |
| #1595 / #1597 / #1598 / #1600 / #1607 | **ML roadmap v0** — rollups for device + AI metric history, DB-context fanout fix, control hardening | Metric history charts populate from rollups; metric worker doesn't poison pool |
| #1482 (#1372) | CHECK constraints on authenticator assurance columns | Migration applies idempotently |
| #1622 (#1373) | AssuranceDecision discriminated union | Authenticator decision typing |
| #1473 (#1412) | `ANTHROPIC_BASE_URL` configurable for self-hosted | Self-host AI backend override |
| #1603 | undici 7.28 + nodemailer 9.0.1 (Trivy HIGH) | Email send + outbound HTTP still work |

## 🟠 Agent / installer / Windows (needs a real agent + Windows host)

| PR | Change | Verify |
|---|---|---|
| #1613 (#1481) | Self-heal ProgramData ACL drift when MSI hardening blocked | Drift recovery on Windows |
| #1480 (#1479) | HardenProgramDataAcl run **deferred as LocalSystem** | Interactive double-click + UAC install no longer rolls back (1722/1603) |
| #1477 (#1470) | Installer pre-flights `/api` version-metadata, not apex `/health` | Install succeeds behind proxy that only routes `/api/*` |
| #1478 (#1472) | Clear `pendingReboot` from live OS signal after reboot | Patch → reboot → flag clears |
| #1524 | **Windows hardware reporting** — BIOS, GPU, + motherboard details | Agent reports correct hardware (also has a UI side, below) |

---

## 🖱️ UI e2e / Playwright + manual testing

User-facing UI changes to click-test in the browser. Big new surfaces first.

### Quotes / Proposals (brand-new feature, 3 phases — test the whole funnel)
- **#1455 (P1)** — block-based proposal editor (one-time/monthly/annual blocks), PDF generation
- **#1468 (P2)** — send a quote → **public/portal view** → **e-sign accept → convert to invoice**
- **#1483 (P3)** — quote **expiry** (UTC day-boundary) + **accept → pay via Stripe** (auto-issue-on-accept, one-shot pay URL)
- **Flow:** create quote with mixed block types → preview PDF → send → open public link in a fresh browser context → accept/e-sign → confirm conversion to invoice → pay → confirm expired quotes show 410. Watch known traps: `<img src>` 401 (blob-fetch), same-org cross-quote image IDOR, at-most-once convert.

### Billing / Catalog / Invoices
- **#1467 (#1368)** — complete **billing-catalog UI**: per-org pricing, ticket-part link, archive confirm
- **#1460 (#1418)** — unambiguous **in-flight state when issuing an invoice** (stale header bug)
- **#1610** — per-partner **Stripe API key** entry UI (replaces Connect onboarding)
- ⚠️ Watch the **billing UI ↔ Zod payload gotcha** (`null`/`Number()` vs `undefined`/money-string) that broke #1411 in-browser but passed tests — submit real money values.

### RBAC / permissions-aware UI
- **#1454** — dedicated **billing roles**, tightened technician grants, permission-aware UI. Log in as a technician and a billing-only role; confirm buttons/tabs hide and gated routes 403 cleanly.

### Authenticator
- **#1433** — **registration redesign** ("the phone just works", no setup; PIN → L4 re-auth). Full enrollment + step-up re-auth on an approval.

### Devices
- **#1462 (#1386)** — Role and Type columns **no longer duplicate** on agent rows
- **#1461 (#1459)** — **server-side software-name search** in the filter picker (type-ahead)
- **#1390** — **Watchdog version** shown in device UI
- **#1524 (web side)** — BIOS / GPU / motherboard hardware fields render on device detail
- **#1590** — multi-select **"Run Script"** no longer sends empty `deviceIds` (was 400). Select multiple devices → Run Script.

### Scripts / Script-AI editor
- **#1593 (#1589)** — Monaco theme colors preserved across **View-Transition** swap (navigate away and back)
- **#1453** — restore **Script AI assistant editor insertion**
- **#1457** — script-builder **library tools** wired through session guardrail (list/search scripts from AI)
- **#1469** — availability picker **gated on JWT scope, not partners list** — ⚠️ test as a **multi-org partner user** (the `isPartnerScope = partners.length>0` bug hid the feature from its own audience)

### Update Ring / patches
- **#1456** — **Update Ring editor redesigned** as a unified approval matrix (exercise approve/defer/decline across the matrix)

### Alert templates
- **#1466 (#1425)** — **partner-wide alert templates** API + management UI. ⚠️ Same multi-org-partner gating trap — confirm a multi-org partner can still create.

### Portal
- **#1474** — customer portal deployed under the **`/portal` path prefix** (the commit *title* says `/c`, but the body + `PORTAL_BASE_PATH=/portal` + `docker/Caddyfile.prod` confirm the real prefix is `/portal`; `/c` only survives as a test fixture). ⚠️ Astro node-standalone base-optional gotcha (un-based routes served 200) — verify portal routes only resolve under `/portal`.

### Patching — full-area sweep (added 2026-06-19, not version-scoped)
Walk the **entire** Patching surface like a daily MSP user and note UX clunkiness as you go:
- **Patching page** — Compliance tab, Patches tab, filters (incl. 3rd-party source), per-device patch list, run a scan, approve/reject a patch.
- **Update Rings** — the redesigned approval matrix (#1456): create/edit a ring, approve/defer/decline across the matrix.
- **Config Policy** — patch-related policy: create a policy, link the patch feature, assign to org/site, preview effective config, create a patch job from a policy.
- **Tabs / navigation** between all the above; **approvals** flow end-to-end.
- There is now one live enrolled Windows device (`WIN-DHQNR1F8LO2`) so patch compliance/lists have real data.
- Log functional PASS/FAIL **and** a running UX-clunkiness list (confusing labels, dead-ends, missing feedback, layout/overlay issues, console noise).

### Misc UI / polish
- **#1463 (#1419)** — collapsed **AI + Documentation side panels** are inert (can't tab into hidden controls)
- **#1602 (#1601)** — FormData uploads get a **10-min timeout** (large upload doesn't 30s-timeout)
- **#1592** — docs site **CTA banner** pointing back to product (marketing site)

---

## Excluded — no manual test needed
Pure tooling / CI / dep / docs / test-only, already CI-covered: #1623, #1620 (wt-stack), #1621/#1604/#1619 (test fixes), #1611/#1475/#1458/#1439 (docs/plans), #1605/#1606 (CI), #1561–#1564 (Tauri dep bumps), #1464 (pnpm action bump).

---

**Bottom line:** ~21 PRs touch the browser UI and warrant a Playwright/manual pass. Largest new surfaces: **Quotes (3 phases)**, the **Stripe-key billing rework**, **billing RBAC**, **Update Ring redesign**, **Authenticator registration redesign**. The zod v4 upgrade is the sleeper regression risk — silent breaks, so it needs a broad form-submission sweep.
